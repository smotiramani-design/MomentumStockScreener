# Core
pandas>=2.0
requests>=2.31
python-dotenv>=1.0       # loads .env file automatically

# Market data
yfinance>=0.2.40

# Scheduling
pytz>=2024.1

# SMS providers (install only what you use)
# twilio>=8.0            # pip install twilio
# boto3>=1.34            # pip install boto3      (AWS SNS)
# vonage>=3.0            # pip install vonage
